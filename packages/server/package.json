{
  "name": "@kquiz/server",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "Thomas Sargent",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/server.ts"
  },
  "scripts": {
    "build:client": "npm --prefix ../client run build",
    "start": "npm run build:client && node src/index.ts",
    "dev": "npm run build:client && NODE_ENV=dev node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^5.2.1",
    "uuid": "^14.0.1",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^26.2.0",
    "@types/uuid": "^10.0.0",
    "@types/ws": "^8.18.1",
    "typescript": "^7.0.2"
  }
}
